# School-Automation-java
To compile project go to project directory and run the command below.
  javac Main.java myPackage/*.java
  
To run project go to project directory and run the command below.
  java Main
